# tanutus-android-keyboard

## このプロジェクトは何か
Android用ソフトウェアキーボード(IME)。自分用に始めたが、Google Playで公開し、
買い切り課金(Pro解放)で収益化することを目標にしている。
ターゲット: Markdownやコードを書く日本語話者(Obsidian / Notion / GitHub / Termuxユーザー)。
売りは「日本語変換 + 記号・Markdownが速い」を一枚のキーボードで成立させること。

## 製品方針(変更するときは必ず人に確認)
- INTERNET権限は持たない。ネット通信が必要になる提案は実装前に相談する。
- 課金はサブスクではなく買い切り(Play Billingの一回購入)。
  無料版でも日本語入力とレイヤー1/2は制限なく使える。Proに置くのは
  Markdown補助動作・自作レイアウト・スニペット。
- 見た目(テーマ・配色)より入力体験を優先する。テーマ機能は後回し。
- 「万人向け」にしない。上記ターゲットに刺さる機能を優先する。

## 機能の優先順位
1. 日本語変換(ローマ字→かな→漢字)の安定動作。ここが動かないと商品にならない。
2. Markdown補助動作: `**`/`` ` ``のペア入力とカーソル配置、リスト行でのEnter継続、
   `- [ ]`や```` ``` ````のワンタップ、見出しレベルのトグル。
3. レイアウトの編集とJSONでのエクスポート/インポート。
4. Ctrl / Alt / Esc(Termux向け)。既存のTab・矢印スワイプと同じ設計思想で。
5. クリップボード履歴とスニペット登録。

## 進め方
- このファイルより実コードのほうが正しい。矛盾があればコードを信じ、
  このファイルの修正を提案する。
- 大きな設計変更・依存ライブラリ追加・権限追加は、実装前に一言で方針確認する。
- 変更後は `./gradlew assembleDebug` が通ることを確認してから報告する。
- セッションの終わりに docs/PROGRESS.md へ追記する
  (日付 / やったこと / 動作確認の結果 / 次にやること)。次のセッションはここから読む。
- 報告・コメント・コミットメッセージは日本語で。
